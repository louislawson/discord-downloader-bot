[pytest]
asyncio_mode = auto
testpaths = tests
filterwarnings =
    # Discord.py + Pydantic deprecation noise from our deps — silence so
    # real test failures aren't lost in the scroll.
    ignore::DeprecationWarning:discord.*
    ignore::DeprecationWarning:pydantic.*
